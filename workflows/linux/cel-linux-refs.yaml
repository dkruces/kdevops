---
configs:
  - next:
    config: BOOTLINUX_CEL_LINUX_NEXT
    ref: "nfsd-next"
    help: "Pull the nfsd-next branch."
  - fixes:
    config: BOOTLINUX_CEL_LINUX_FIXES
    ref: "nfsd-fixed"
    help: "Pull the nfsd-fixed branch."
  - custom:
    config: BOOTLINUX_CEL_LINUX_CUSTOM
    ref: BOOTLINUX_CEL_LINUX_CUSTOM_NAME
    help: "Use a custom branch name."
