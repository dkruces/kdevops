# Use manually-built development QEMU from /usr/local/bin
# This causes kconfig to automatically set the correct binary paths
# for the host architecture (ARM64 in this case)
CONFIG_QEMU_USE_DEVELOPMENT_VERSION=y
CONFIG_QEMU_BIN_PATH_LIBVIRT="/usr/local/bin/qemu-system-aarch64"
