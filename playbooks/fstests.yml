---
- name: Verify the fstests localhost prep deps and isolate the sudo opt-in
  hosts: localhost
  tasks:
    - name: Run the non-sudo verify
      ansible.builtin.include_role:
        name: fstests/runtime_deps/verify

    - name: Run the sudo opt-in setup (only with --tags fstests_runtime_deps_setup)
      ansible.builtin.include_role:
        name: fstests/runtime_deps/setup
        apply:
          tags: [fstests_runtime_deps_setup]
      tags: [never, fstests_runtime_deps_setup]

- name: Configure and run the filesystem testing suite workflow
  hosts: baseline:dev
  roles:
    - role: fstests
