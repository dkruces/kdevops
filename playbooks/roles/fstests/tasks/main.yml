---
- name: Import optional extra_args file
  ansible.builtin.include_vars: "{{ item }}"
  failed_when: false
  with_first_found:
    - files:
        - "../extra_vars.yml"
        - "../extra_vars.yaml"
        - "../extra_vars.json"
      skip: true
  tags: vars

- name: Set up the /data partition the fstests workflow stores trees in
  ansible.builtin.include_role:
    name: create_data_partition
  tags: ["oscheck", "data_partition"]

# Distro specific
- name: Install dependencies
  ansible.builtin.include_tasks: install-deps/main.yml
- name: Check if there's an existing xfsprogs directory
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.stat:
    path: "{{ fstests_xfs_xfsprogs_data }}"
  register: fstests_xfsprogs_dir
  tags: ["oscheck", "git", "fstests"]
  when: "fstests_xfs_build_custom_xfsprogs|bool"

- name: Set up an iSCSI initiator on target nodes
  ansible.builtin.include_role:
    name: iscsi
    tasks_from: add_initiator
  when:
    - fstests_nfs_section_pnfs|bool

- name: Infer the data user and group from the common role
  ansible.builtin.include_role:
    name: common
  when:
    - fstests_infer_uid_and_group|bool

- name: Get fstests_nproc
  tags: ["oscheck", "fstests", "build"]
  ansible.builtin.command: "nproc"
  register: fstests_nproc
  changed_when: false

- name: Ensure xfsprogs is not root owned
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    path: "{{ fstests_xfs_xfsprogs_data }}"
    state: directory
    recurse: true
    follow: false
    owner: "{{ data_user }}"
    group: "{{ data_group }}"
    mode: "0755"
  tags: ["oscheck", "git", "fstests", "do_deploy"]
  when:
    - fstests_xfs_build_custom_xfsprogs|bool
    - fstests_xfsprogs_dir.stat.exists

- name: Clean old xfsprogs build
  ansible.builtin.command: "git clean -f -x -d"
  args:
    chdir: "{{ fstests_xfs_xfsprogs_data }}"
  tags: ["oscheck", "git", "fstests"]
  when:
    - fstests_xfs_build_custom_xfsprogs|bool
    - fstests_xfsprogs_dir.stat.exists
  changed_when: true

- name: Clone xfsprogs git repository
  ansible.builtin.git:
    repo: "{{ fstests_xfs_xfsprogs_git }}"
    dest: "{{ fstests_xfs_xfsprogs_data }}"
    update: true
    version: "{{ fstests_xfs_xfsprogs_commit }}"
  retries: 3
  delay: 5
  register: fstests_result
  until: not fstests_result.failed
  tags: ["oscheck", "git", "fstests", "do_fetch"]
  when: "fstests_xfs_build_custom_xfsprogs|bool"

- name: Configure xfsprogs
  community.general.make:
    chdir: "{{ fstests_xfs_xfsprogs_data }}"
    target: configure
  tags: ["oscheck", "fstests", "build", "do_configure"]
  when: "fstests_xfs_build_custom_xfsprogs|bool"

- name: Build xfsprogs
  community.general.make:
    chdir: "{{ fstests_xfs_xfsprogs_data }}"
    jobs: "{{ fstests_nproc.stdout }}"
  tags: ["oscheck", "fstests", "build", "do_compile"]
  when: "fstests_xfs_build_custom_xfsprogs|bool"

- name: Install xfsprogs
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  community.general.make:
    target: install
    params:
      DIST_ROOT: "{{ fstests_xfs_xfsprogs_install_path }}"
    chdir: "{{ fstests_xfs_xfsprogs_data }}"
  tags: ["oscheck", "fstests", "build", "do_compile"]
  when: "fstests_xfs_build_custom_xfsprogs|bool"

- name: Check if there's an existing xfsdump directory
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.stat:
    path: "{{ fstests_xfsdump_data }}"
  register: fstests_xfdump_dir
  tags: ["oscheck", "git", "fstests"]
  when:
    - fstests_xfsdump_build|bool

- name: Ensure xfsdump is not root owned
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    path: "{{ fstests_xfsdump_data }}"
    recurse: true
    owner: "{{ data_user }}"
    group: "{{ data_group }}"
    mode: "0644"
  tags: ["fstests", "deps"]
  when:
    - fstests_xfsdump_build|bool
    - fstests_xfdump_dir.stat.exists

- name: Clean old xfsdump build
  ansible.builtin.command: "git clean -f -x -d"
  args:
    chdir: "{{ fstests_xfsdump_data }}"
  tags: ["oscheck", "git", "fstests"]
  when:
    - fstests_xfsdump_build|bool
    - fstests_xfdump_dir.stat.exists
  changed_when: true

- name: Git clone xfsdump
  ansible.builtin.git:
    repo: "{{ fstests_xfsdump_git }}"
    dest: "{{ fstests_xfsdump_data }}"
    update: true
    version: "{{ fstests_xfsdump_commit }}"
  tags: ["fstests", "deps", "do_fetch"]
  when:
    - fstests_xfsdump_build|bool

- name: Build xfsdump
  tags: ["fstests", "deps", "do_compile"]
  community.general.make:
    chdir: "{{ fstests_xfsdump_data }}"
    jobs: "{{ fstests_nproc.stdout }}"
  when:
    - fstests_xfsdump_build|bool

- name: Install xfsdump
  become: true
  become_method: ansible.builtin.sudo
  tags: ["fstests", "deps", "do_compile"]
  community.general.make:
    chdir: "{{ fstests_xfsdump_data }}"
    target: install
  when:
    - fstests_xfsdump_build|bool

- name: Setup $PATH environment variable to access custom xfsprogs binaries
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.lineinfile:
    path: "/root/.bashrc"
    line: "export PATH={{ fstests_xfs_xfsprogs_install_path }}/usr/sbin/:$PATH"
    state: present
  tags: ["oscheck", "fstests", "build", "do_configure"]
  when: "fstests_xfs_build_custom_xfsprogs|bool"

- name: Set up the fstests sparsefiles partition
  ansible.builtin.include_role:
    name: create_partition
  vars:
    create_partition_disk_device: "{{ fstests_sparsefiles_device }}"
    create_partition_disk_fstype: "{{ fstests_sparsefiles_fstype }}"
    create_partition_disk_label: "{{ fstests_sparsefiles_label }}"
    create_partition_disk_fs_opts: "{{ fstests_sparsefiles_fs_opts }}"
    create_partition_disk_path: "{{ fstests_sparsefiles_path }}"
    create_partition_disk_user: "{{ data_user }}"
    create_partition_disk_group: "{{ data_group }}"
  tags: ["oscheck", "sparse_partition"]
  when:
    - fstests_testdev_sparsefile_generation|bool

- name: Git clone kdevops
  environment:
    GIT_SSL_NO_VERIFY: "true"
  ansible.builtin.git:
    repo: "{{ kdevops_git }}"
    dest: "{{ kdevops_data }}"
    version: "{{ kdevops_git_version }}"
  retries: 3
  delay: 5
  register: fstests_result
  until: not fstests_result.failed
  tags: ["oscheck", "install", "git", "do_fetch"]

- name: Copy over our .config
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/../.config"
    dest: "{{ kdevops_dir }}"
    mode: "0644"
  tags: ["oscheck", "install", "root", "config", "do_deploy"]

- name: Install kdevops as root
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  environment:
    FSTYP: "{{ fstests_fstyp }}"
  ansible.builtin.command: "{{  make }} install"
  tags: ["oscheck", "install", "root"]
  args:
    chdir: "{{ kdevops_dir }}"

# We do this *after* the /data/ partition is set up so
# we can house the dbench source code there and so that
# we don't have to bother with the setup of that partition
# in the dbench role.
  changed_when: true
- name: Compile dbench from source if needed by the distribution
  ansible.builtin.include_role:
    name: compile_dbench
  vars:
    compile_dbench_dbench_data: "{{ data_path }}/dbench"
    compile_dbench_dbench_git: "https://github.com/linux-kdevops/dbench.git"
  tags: ["oscheck", "git", "dbench"]

- name: Remove any old fstests and install directories
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
    mode: "0644"
  with_items:
    - "{{ fstests_data }}"
    - "{{ fstests_data_prefix }}"
    - "{{ fstests_data_target }}"
  tags: ["oscheck", "git", "fstests", "build", "do_clean"]

- name: Git clone fstests
  ansible.builtin.git:
    repo: "{{ fstests_git }}"
    dest: "{{ fstests_data }}"
    version: "{{ fstests_git_version }}"
    update: true
  retries: 3
  delay: 5
  register: fstests_result
  until: not fstests_result.failed
  tags: ["oscheck", "git", "fstests", "do_fetch"]

- name: Create the fstests data-prefix directory if it does not exist
  tags: ["oscheck", "fstests", "install", "root", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    path: "{{ fstests_data_prefix }}"
    state: directory
    mode: "0755"

- name: Build the configure file for fstests
  tags: ["oscheck", "fstests", "build"]
  ansible.builtin.command: "make configure"
  args:
    chdir: "{{ fstests_data }}"
  changed_when: true

- name: Configure fstests
  tags: ["oscheck", "fstests", "build"]
  ansible.builtin.command: "./configure --prefix={{ fstests_data_prefix }}"
  args:
    chdir: "{{ fstests_data }}"
  changed_when: true

- name: Get fstests_nproc
  tags: ["oscheck", "fstests", "build"]
  ansible.builtin.command: "nproc"
  register: fstests_nproc
  changed_when: false

- name: Build fstests
  tags: ["oscheck", "fstests", "build", "do_compile"]
  community.general.make:
    chdir: "{{ fstests_data }}"
    jobs: "{{ fstests_nproc.stdout }}"

- name: Copy udev rules for zoned
  become: true
  become_method: ansible.builtin.sudo
  ansible.builtin.template:
    src: "64-btrfs-zoned.rules"
    dest: "/usr/lib/udev/rules.d/"
    owner: "{{ data_user }}"
    group: "{{ data_group }}"
    mode: "0644"
  tags: ["oscheck", "fstests", "do_deploy"]
  when:
    - fstests_zns_enabled == "y"
    - fstests_fstyp == "btrfs"

- name: Git pull latest kdevops to ensure expunge lists and scripts are up to date
  environment:
    GIT_SSL_NO_VERIFY: "true"
  ansible.builtin.git:
    repo: "{{ kdevops_git }}"
    dest: "{{ kdevops_data }}"
    version: "{{ kdevops_git_version }}"
    update: true
  retries: 3
  delay: 5
  register: fstests_result
  until: not fstests_result.failed
  tags: ["oscheck", "install", "git", "git_update", "fstests", "run_tests", "do_fetch"]

- name: Install fstests
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.command: "{{  make }} install"
  tags: ["oscheck", "fstests", "install", "root"]
  args:
    chdir: "{{ fstests_data }}"
  changed_when: true

- name: Add symlink for old fstests directory
  tags: ["oscheck", "fstests", "install", "root", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    src: "{{ fstests_data_prefix }}/xfstests"
    dest: "{{ fstests_data_target }}"
    state: link

- name: Install root generated .fstests_setup
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.template:
    src: "{{ fstests_setup }}"
    dest: "/root/{{ fstests_setup }}"
    owner: "root"
    group: "root"
    mode: "0644"

- name: Source .fstests_setup for root if it is not already done
  vars:
    dev_bash_config: ".bashrc"
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install"]
  ansible.builtin.shell: |
    SOURCE_KDEVOPS_FSTESTS_SETUP="test -s ~/{{ fstests_setup }} && . ~/{{ fstests_setup }} || true"
    if [ -f {{ dev_bash_config }} ]; then
      if ! grep "{{ fstests_setup_name }}" {{ dev_bash_config }}  2>&1 > /dev/null ; then
        echo "$SOURCE_KDEVOPS_FSTESTS_SETUP" >> {{ dev_bash_config }}
      fi
    else
      echo "#!/bin/bash" >> {{ dev_bash_config }}
      echo "$SOURCE_KDEVOPS_FSTESTS_SETUP" >> {{ dev_bash_config }}
      chmod 755 {{ dev_bash_config }}
    fi
  args:
    executable: /bin/bash
  changed_when: true

- name: Add symlinks for kdevops scripts for fstests
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    src: "{{ kdevops_data }}/workflows/fstests/scripts/{{ item }}"
    dest: "{{ fstests_data_target }}/{{ item }}"
    state: link
  with_items:
    - gendisks.sh
    - oscheck.sh
    - naggy-check.sh

- name: Create a few directories which kdevops uses for fstests if they do not exist
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  with_items:
    - "{{ fstests_sparsefiles_path }}"
  when:
    - fstests_testdev_sparsefile_generation|bool

- name: Create a few directories which kdevops uses for fstests if they do not exist
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  with_items:
    - "{{ fstests_data_target }}/configs"

- name: Check if a host-specific fstests config exists for this section
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  delegate_to: localhost
  ansible.builtin.stat:
    path: "{{ topdir_path }}/workflows/fstests/{{ fstests_fstyp }}/{{ inventory_hostname }}.config"
  register: fstests_host_specific_fstests_config

- name: Generate filesystem configuration file for debugging purposes using host specific config {{ target_config }}
  tags: ["generate-fstests-config", "do_deploy"]
  vars:
    target_config: "{{ topdir_path }}/workflows/fstests/{{ fstests_fstyp }}/{{ fstests_fstyp }}.config.generated"
  ansible.builtin.template:
    src: "{{ topdir_path }}/workflows/fstests/{{ fstests_fstyp }}/{{ ansible_host }}.config"
    dest: "{{ target_config }}"
    force: true
    mode: "0644"
  when:
    - fstests_generate_simple_config_enable|bool
    - fstests_debug_localhost|bool
    - fstests_host_specific_fstests_config.stat.exists

- name: Generate filesystem configuration file for debugging purposes using generic config {{ target_config }}
  tags: ["generate-fstests-config", "do_deploy"]
  vars:
    target_config: "{{ topdir_path }}/workflows/fstests/{{ fstests_fstyp }}/{{ fstests_fstyp }}.config.generated"
  ansible.builtin.template:
    src: "{{ fstests_fstyp }}/{{ fstests_fstyp }}.config"
    dest: "{{ topdir_path }}/workflows/fstests/{{ fstests_fstyp }}/{{ fstests_fstyp }}.config.generated"
    force: true
    mode: "0644"
  when:
    - fstests_generate_simple_config_enable|bool
    - fstests_debug_localhost|bool
    - not fstests_host_specific_fstests_config.stat.exists

- name: Copy and force overwrite of the host specific fstests configuration
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.template:
    src: "{{ topdir_path }}/workflows/fstests/{{ fstests_fstyp }}//{{ ansible_host }}.config"
    dest: "{{ fstests_data_target }}/configs/{{ ansible_host }}.config"
    force: true
    mode: "0644"
  when:
    - fstests_generate_simple_config_enable|bool
    - not fstests_debug_localhost|bool
    - fstests_host_specific_fstests_config.stat.exists

- name: Copy and force overwrite of the target filesystem fstests configuration
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.template:
    src: "{{ fstests_fstyp }}/{{ fstests_fstyp }}.config"
    dest: "{{ fstests_data_target }}/configs/{{ ansible_host }}.config"
    force: true
    mode: "0644"
  when:
    - fstests_generate_simple_config_enable|bool
    - not fstests_debug_localhost|bool
    - not fstests_host_specific_fstests_config.stat.exists

- name: Try to see if NVMe EUIs exist on /dev/disk/by-id/nvme-eui.*
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: /dev/disk/by-id/
    file_type: "link"
    patterns: "nvme-eui.*"
  register: fstests_real_nvme_euis
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  when:
    - fstests_generate_nvme_live_config_enable|bool

- name: End if no NVMe EUIs found per node and no fallback mechanism is provided
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.fail:
    msg:
      - "You need at least one NVMe drive with its EUI defined per node, try enabling CONFIG_FSTESTS_TESTDEV_NVME_FALLBACK_MODEL_SERIAL"
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - not fstests_testdev_nvme_fallback_model_serial|bool
    - fstests_real_nvme_euis is success
    - fstests_real_nvme_euis.files is defined
    - fstests_real_nvme_euis.files | length == 0

- name: Set real fstests_nvmes if one is found from EUI list
  ansible.builtin.set_fact:
    fstests_nvmes: "{{ fstests_real_nvme_euis }}"
  when:
    - fstests_generate_nvme_live_config_enable|bool
    - fstests_real_nvme_euis is success
    - fstests_real_nvme_euis.files is defined
    - fstests_real_nvme_euis.files | length > 0
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]

- name: Fallback look for NVMe drive by model and serial number /dev/disk/by-id/nvme-$(model)-$(serial)
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: /dev/disk/by-id/
    file_type: "link"
    patterns: "nvme-*"
    excludes: "nvme-QEMU*,nvme-nvme*"
  register: fstests_fallback_nvmes
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  when:
    - fstests_real_nvme_euis is success
    - fstests_real_nvme_euis.files is defined
    - fstests_real_nvme_euis.files | length == 0
    - fstests_generate_nvme_live_config_enable|bool
    - fstests_testdev_nvme_fallback_model_serial|bool

- name: Set real fstests_nvmes to fallback list if one found
  ansible.builtin.set_fact:
    fstests_nvmes: "{{ fstests_fallback_nvmes }}"
  when:
    - fstests_generate_nvme_live_config_enable|bool
    - fstests_real_nvme_euis is success
    - fstests_real_nvme_euis.files is defined
    - fstests_real_nvme_euis.files | length == 0
    - fstests_testdev_nvme_fallback_model_serial|bool
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]

- name: Get NVMe drives we can use for testing
  ansible.builtin.set_fact:
    fstests_nvme_symlinks: |
      {%- set x = [] -%}
      {%- for f in fstests_nvmes.files %}
      {{ x.append(f.path) }}
      {%- endfor -%}
      {{ x }}
  when:
    - fstests_generate_nvme_live_config_enable|bool
    - fstests_nvmes is defined
    - fstests_nvmes is success
    - fstests_nvmes.files is defined
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]

- name: Read existing partition information for first NVMe drive with an EUI
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  community.general.parted:
    device: "{{ fstests_nvme_symlinks[0] }}"
    unit: MiB
  register: fstests_nvme_info
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - fstests_nvme_symlinks | length >= 1

- name: Remove all partitions from the first real NVMe drive with an EUI
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  community.general.parted:
    device: "{{ fstests_nvme_symlinks[0] }}"
    number: "{{ item.num }}"
    state: absent
  loop: "{{ fstests_nvme_info.partitions }}"
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - fstests_nvme_symlinks | length > 1
    - fstests_nvme_info.partitions | length != 13

- name: Create extended partition on sigle NVMe drive
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  community.general.parted:
    device: "{{ fstests_nvme_symlinks[0] }}"
    number: 1
    state: present
    part_type: "extended"
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - fstests_nvme_symlinks | length >= 1
    - fstests_nvme_info.partitions | length < 13

- name: Create 12 logical partitions on sigle NVMe drive
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  vars:
    test_partsize: 60
    fuzz: 1
  community.general.parted:
    device: "{{ fstests_nvme_symlinks[0] }}"
    number: "{{ item }}"
    state: present
    part_type: "logical"
    part_start: "{{ ((item - 5) * test_partsize) + 1 }}GiB"
    part_end: "{{ ((item - 4) * test_partsize) }}GiB"
  loop: "{{ range(5, 17) | list }}"
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - fstests_nvme_symlinks | length >= 1
    - fstests_nvme_info.partitions | length < 13

- name: Try to see if new logical NVMe partition exist on /dev/disk/by-id/nvme-*.id.part*
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  vars:
    nvme_main: "{{ fstests_nvme_symlinks[0] | basename }}"
  ansible.builtin.find:
    paths: /dev/disk/by-id/
    file_type: "link"
    patterns: "{{ nvme_main }}-part*"
  register: fstests_part_symlinks
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - fstests_nvme_symlinks | length >= 1

- name: Get new partition NVMe symlinks
  ansible.builtin.set_fact:
    fstests_nvme_parts: |
      {%- set x = [] -%}
      {%- for f in fstests_part_symlinks.files %}
      {{ x.append(f.path) }}
      {%- endfor -%}
      {{ x }}
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - fstests_part_symlinks is success
    - fstests_part_symlinks.files is defined
    - fstests_nvme_symlinks | length >= 1
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]

- name: End if 13 paritions NVMe EUIs not found per node one extended 12 logical
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.fail:
    msg: >-
      You need at least one extended partition and 12 logical partitions NVMe drives
      per guest. Detected {{ fstests_nvme_parts | length }} NVMe drives. Bailing test.
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - fstests_nvme_symlinks | length >= 1
    - fstests_nvme_parts | length < 13

- name: Copy and force overwrite of the target filesystem fstests configuration using NVMe partition euis
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  vars:
    fstests_test_dev: "{{ fstests_nvme_symlinks[0] }}-part16"
    fstests_logwrites_dev: "{{ fstests_nvme_symlinks[0] }}-part15"
    fstests_scratch_devpool: >-
      {{ fstests_nvme_symlinks[0] }}-part5
      {{ fstests_nvme_symlinks[0] }}-part6
      {{ fstests_nvme_symlinks[0] }}-part7
      {{ fstests_nvme_symlinks[0] }}-part8
      {{ fstests_nvme_symlinks[0] }}-part9
      {{ fstests_nvme_symlinks[0] }}-part10
      {{ fstests_nvme_symlinks[0] }}-part11
      {{ fstests_nvme_symlinks[0] }}-part12
    fstests_scratch_logdev: "{{ fstests_nvme_symlinks[0] }}-part15"
    fstests_scratch_rtdev: "{{ fstests_nvme_symlinks[0] }}-part14"
  ansible.builtin.template:
    src: "{{ fstests_fstyp }}/{{ fstests_fstyp }}.config"
    dest: "{{ fstests_data_target }}/configs/{{ ansible_host }}.config"
    force: true
    mode: "0644"
  when:
    - fstests_testdev_nvme_partition_euis|bool
    - fstests_nvme_symlinks | length >= 1
    - fstests_nvme_parts | length >= 13

# fstests sparsefile generation uses 11 loop drives, we'll aim for 7
# if you are using straight a drive per target disk to test.
- name: End running if 7 NVMe EUIs not found per node
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.fail:
    msg:
      - "You need at least 7 NVMe drives per guest. Detected {{ fstests_nvme_symlinks | length }} NVMe drives. Bailing test."
  when:
    - fstests_testdev_nvme_euis|bool
    - fstests_nvme_symlinks | length < 7

- name: Copy and force overwrite of the target filesystem fstests configuration using NVMe euis
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  vars:
    fstests_test_dev: "{{ fstests_nvme_symlinks[0] }}"
    fstests_logwrites_dev: "{{ fstests_nvme_symlinks[1] }}"
    fstests_scratch_devpool: >-
      {{ fstests_nvme_symlinks[2] }}
      {{ fstests_nvme_symlinks[3] }}
      {{ fstests_nvme_symlinks[4] }}
      {{ fstests_nvme_symlinks[5] }}
      {{ fstests_nvme_symlinks[6] }}
  ansible.builtin.template:
    src: "{{ fstests_fstyp }}/{{ fstests_fstyp }}.config"
    dest: "{{ fstests_data_target }}/configs/{{ ansible_host }}.config"
    force: true
    mode: "0644"
  when:
    - fstests_testdev_nvme_euis|bool
    - fstests_nvme_symlinks | length >= 7

- name: Set the export volname prefix
  tags: vars
  ansible.builtin.set_fact:
    fstests_volname_prefix: "{{ (ansible_host | default(inventory_hostname)) | regex_replace(kdevops_host_prefix + '-') }}-fs"
  when:
    - fstests_fstyp == "nfs" or fstests_fstyp == "cifs"

- name: Set the NFS export's fstype
  tags: vars
  ansible.builtin.set_fact:
    fstests_nfs_export_fstype: "{{ nfsd_export_fstype | default('xfs') }}"
  when:
    - not fstests_nfs_section_pnfs|bool

- name: Set the NFS export's fstype
  tags: vars
  ansible.builtin.set_fact:
    fstests_nfs_export_fstype: "xfs"
  when:
    - fstests_fstyp == "nfs"
    - fstests_nfs_section_pnfs|bool

- name: Create NFS export for test vol
  ansible.builtin.include_role:
    name: nfsd_add_export
  vars:
    nfsd_add_export_server_host: "{{ fstests_nfs_server_host }}"
    nfsd_add_export_volname: "{{ fstests_volname_prefix }}-t"
    nfsd_add_export_options: "{{ nfsd_export_options }}"
    nfsd_add_export_fstype: "{{ fstests_nfs_export_fstype }}"
    nfsd_add_export_size: 20g
    nfsd_add_export_pnfs: false
  when:
    - fstests_fstyp == "nfs"
    - fstests_nfs_use_kdevops_nfsd|bool

- name: Create NFS export for scratch vol
  ansible.builtin.include_role:
    name: nfsd_add_export
  vars:
    nfsd_add_export_server_host: "{{ fstests_nfs_server_host }}"
    nfsd_add_export_volname: "{{ fstests_volname_prefix }}-s"
    nfsd_add_export_options: "{{ nfsd_export_options }}"
    nfsd_add_export_fstype: "{{ fstests_nfs_export_fstype }}"
    nfsd_add_export_size: 30g
    nfsd_add_export_pnfs: false
  when:
    - fstests_fstyp == "nfs"
    - fstests_nfs_use_kdevops_nfsd|bool

- name: Copy and force overwrite of the target filesystem fstests configuration for NFS
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  vars:
    fstests_nfs_test_dev: "{{ fstests_nfs_server_host }}:{{ nfsd_export_path }}/{{ fstests_volname_prefix }}-t"
    fstests_nfs_scratch_devpool: "{{ fstests_nfs_server_host }}:{{ nfsd_export_path }}/{{ fstests_volname_prefix }}-s"
  ansible.builtin.template:
    src: "{{ fstests_fstyp }}/{{ fstests_fstyp }}.config"
    dest: "{{ fstests_data_target }}/configs/{{ ansible_host }}.config"
    force: true
    mode: "0644"
  when:
    - fstests_fstyp == "nfs"

- name: Check to see if /etc/nfsmount.conf exists
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.stat:
    path: /etc/nfsmount.conf
  register: fstests_nfsmount_conf
  when:
    - fstests_fstyp == "nfs"
    - fstests_nfs_auth_flavor is defined
    - fstests_nfs_auth_flavor

- name: Create /etc/nfsmount.conf
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.template:
    src: "{{ fstests_fstyp }}/nfsmount.conf"
    dest: /etc/nfsmount.conf
    owner: root
    group: root
    mode: "0644"
  when:
    - fstests_fstyp == "nfs"
    - fstests_nfs_auth_flavor is defined
    - fstests_nfs_auth_flavor
    - not fstests_nfsmount_conf.stat.exists

- name: Set auth flavor for NFS
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.lineinfile:
    path: /etc/nfsmount.conf
    regexp: "^# Sec="
    line: "Sec={{ fstests_nfs_auth_flavor }}"
  when:
    - fstests_fstyp == "nfs"
    - fstests_nfs_auth_flavor is defined
    - fstests_nfs_auth_flavor

- name: Create SMB share for test vol
  ansible.builtin.include_role:
    name: smbd_add_share
  vars:
    smbd_add_share_server_host: "{{ fstests_smb_server_host }}"
    smbd_add_share_volname: "{{ fstests_volname_prefix }}-t"
    smbd_add_share_fstype: "{{ smbd_share_fstype }}"
    smbd_add_share_size: 20g
  when:
    - fstests_fstyp == "cifs"
    - fstests_cifs_use_kdevops_smbd|bool

- name: Create SMB share for scratch vol
  ansible.builtin.include_role:
    name: smbd_add_share
  vars:
    smbd_add_share_server_host: "{{ fstests_smb_server_host }}"
    smbd_add_share_volname: "{{ fstests_volname_prefix }}-s"
    smbd_add_share_fstype: "{{ smbd_share_fstype }}"
    smbd_add_share_size: 30g
  when:
    - fstests_fstyp == "cifs"
    - fstests_cifs_use_kdevops_smbd|bool

- name: Copy and force overwrite of the target filesystem fstests configuration for CIFS
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  vars:
    fstests_cifs_test_dev: "//{{ fstests_smb_server_host }}/{{ fstests_volname_prefix }}-t"
    fstests_cifs_scratch_devpool: "//{{ fstests_smb_server_host }}/{{ fstests_volname_prefix }}-s"
  ansible.builtin.template:
    src: "{{ fstests_fstyp }}/{{ fstests_fstyp }}.config"
    dest: "{{ fstests_data_target }}/configs/{{ ansible_host }}.config"
    force: true
    mode: "0644"
  when:
    - fstests_fstyp == "cifs"

- name: Reboot system before our test so we know everything is sane
  tags: ["oscheck", "fstests", "run_tests", "reboot"]
  become: true
  become_method: ansible.builtin.sudo
  ansible.builtin.reboot:
    post_reboot_delay: 10

- name: Make sure loop device support is loaded
  tags: ["oscheck", "fstests", "run_tests"]
  become: true
  become_method: ansible.builtin.sudo
  ansible.builtin.command: "modprobe loop"
  when:
    - kdevops_run_fstests | default(false) | bool
  changed_when: true

- name: Add missing groups for fstests
  tags: ["oscheck", "fstests", "install", "root"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.group:
    name: "{{ item }}"
    state: present
  with_items:
    - sys
  loop_control:
    label: "Ensuring that group {{ item }} exists"

- name: Add missing users for fstests
  tags: ["oscheck", "fstests", "install", "root"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.command: "useradd {{ fstests_badname_arg }} -d /home/{{ item }} -U {{ item }}"
  with_items:
    - 123456-fsgqa
    - fsgqa
    - fsgqa2
    - daemon
    - nobody
  loop_control:
    label: "Ensuring that user and group {{ item }} exists"
  register: fstests_useradd_result
  failed_when: fstests_useradd_result.rc != 0 and fstests_useradd_result.rc != 9
  changed_when: true

- name: Verify the fstests section name exists in the rendered fstests config
  vars:
    fstests_section: >-
      {{ (ansible_host | default(inventory_hostname))
         | regex_replace(kdevops_host_prefix + '-')
         | regex_replace('-dev') | regex_replace('-', '_') }}
  tags: ["oscheck", "fstests", "run_tests", "section", "do_configure"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  register: fstests_section_grep
  failed_when: not fstests_section_grep.changed
  ansible.builtin.lineinfile:
    path: "{{ fstests_data_target }}/configs/{{ ansible_host }}.config"
    regexp: "\\[{{ fstests_section }}\\]$"
    line: ""
  check_mode: true
  when:
    - kdevops_run_fstests | default(false) | bool
    - fstests_section != "all"

# We have to set the environment variables explicitly because some distros
# or systems might have on /etc/sudoers env_reset enabled, which gives us
# as root a clean slate of variables.
- name: Generate loopback disks using sparefiles with gendisk.sh
  tags: ["oscheck", "fstests", "run_tests", "gendisks"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.command: "./gendisks.sh -d -m"
  register: fstests_gendisk_cmd
  args:
    chdir: "{{ fstests_data_target }}"
  environment:
    FSTYP: "{{ fstests_fstyp }}"
    FSTESTS_SPARSE_FILE_PATH: "{{ fstests_sparsefiles_path }}"
    FSTESTS_SETUP_SYSTEM: "{{ fstests_setup_system }}"
    FSTESTS_TESTDEV_SPARSEFILE_GENERATION: "{{ 'y' if fstests_testdev_sparsefile_generation | default(false) | bool else 'n' }}"
    FSTESTS_SPARSE_FILE_SIZE: "{{ fstests_sparsefiles_size }}"
    FSTESTS_SPARSE_FILENAME_PREFIX: "{{ fstests_sparsefiles_filename_prefix }}"
    TEST_DEV: "{{ fstests_test_dev }}"
    TEST_DIR: "{{ fstests_test_dir }}"
  when:
    - fstests_testdev_sparsefile_generation|bool
  changed_when: true

- name: Capture gendisk output
  tags: ["oscheck", "fstests", "run_tests", "gendisks"]
  ansible.builtin.debug:
    msg: "{{ fstests_gendisk_cmd.stdout_lines }}"
  when:
    - kdevops_run_fstests | default(false) | bool
    - fstests_debug|bool
    - fstests_testdev_sparsefile_generation|bool

- name: Check and verify fstests dependencies are met prior to running fstests
  vars:
    fstests_section: >-
      {{ (ansible_host | default(inventory_hostname))
         | regex_replace(kdevops_host_prefix + '-')
         | regex_replace('-dev') | regex_replace('-', '_') }}
  tags: ["oscheck", "fstests", "run_tests"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.command: |
    ./oscheck.sh
      --check-deps
      --journal {{ fstests_journal }}
      --test-section {{ fstests_section }}
  args:
    chdir: "{{ fstests_data_target }}"
  environment:
    FSTYP: "{{ fstests_fstyp }}"
    FSTESTS_SPARSE_FILE_PATH: "{{ fstests_sparsefiles_path }}"
    FSTESTS_SETUP_SYSTEM: "{{ fstests_setup_system }}"
    FSTESTS_TESTDEV_SPARSEFILE_GENERATION: "{{ 'y' if fstests_testdev_sparsefile_generation | default(false) | bool else 'n' }}"
    FSTESTS_SPARSE_FILE_SIZE: "{{ fstests_sparsefiles_size }}"
    FSTESTS_SPARSE_FILENAME_PREFIX: "{{ fstests_sparsefiles_filename_prefix }}"
    TEST_DEV: "{{ fstests_test_dev }}"
    TEST_DIR: "{{ fstests_test_dir }}"
  when:
    - kdevops_run_fstests | default(false) | bool
    - fstests_testdev_sparsefile_generation|bool
  changed_when: false

- name: Set the path where we collect our fstests results
  ansible.builtin.set_fact:
    fstests_results_full_path: "{{ topdir_path }}/workflows/fstests/results"
    fstests_workflow_dir: "../workflows/fstests"
    fstests_results_target: "../workflows/fstests/results/last-run/"
  tags: ["oscheck", "fstests", "run_tests", "checktime", "augment_expunge_list", "copy_results", "print_results"]

- name: Look for prior check.time files on {{ fstests_data_target }}
  tags: ["oscheck", "fstests", "run_tests", "checktime"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: "{{ fstests_data_target }}"
    recurse: true
    patterns: "check.time"
  register: fstests_checktime_files

- name: Copy prior check.time files from {{ fstests_data_target }}
  tags: ["oscheck", "fstests", "run_tests", "checktime"]
  ansible.builtin.fetch:
    src: "{{ item.path }}"
    dest: "{{ fstests_workflow_dir }}/{{ item.path | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
    flat: true
    validate_checksum: false
    mode: "0644"
  with_items: "{{ fstests_checktime_files.files }}"
  loop_control:
    label: "{{ item.path | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
  when:
    - fstests_checktime_files.matched > 0

- name: Use check.time files as expunge variables to help with initial baseline
  vars:
    expunge_file: "-E "
    space: " "
  ansible.builtin.set_fact:
    fstests_initial_baseline_args: |
      {%- set x = [] -%}
      {%- for f in fstests_checktime_files.files %}
      {{ x.append(expunge_file + f.path + space) }}
      {%- endfor -%}
      {{ x | join() | trim() }}
  when:
    - fstests_checktime_files is success
    - fstests_checktime_files.files is defined
    - fstests_checktime_files.files | length > 0
    - fstests_initial_baseline|bool
  tags: ["oscheck", "fstests", "run_tests", "checktime"]

- name: Expand tests to skip for initial baseline
  ansible.builtin.set_fact:
    fstests_initial_baseline_args: "--start-after {{ fstests_start_after_test }}"
  when:
    - fstests_start_after|bool
  tags: ["oscheck", "fstests", "run_tests", "checktime"]

- name: Expand tests to skip tests dynamically passed on the command line
  ansible.builtin.set_fact:
    fstests_skip_tests_args: "-e {{ fstests_skip_tests }}"
  when:
    - fstests_skip_tests_enable|bool
  tags: ["oscheck", "fstests", "run_tests", "checktime"]

- name: Verify use of START_AFTER
  tags: ["oscheck", "fstests", "run_tests", "fstests_setup", "install", "generate-fstests-config"]
  ansible.builtin.fail:
    msg:
      - "You used START_AFTER={{ fstests_start_after_test }} and it must not be empty"
  when:
    - fstests_start_after|bool
    - fstests_start_after_test == ""

- name: Clear out old results directory
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    state: absent
    path: "{{ fstests_data_target }}/results/"
    mode: "0644"
  tags: ["oscheck", "fstests", "run_tests", "clean_results", "do_clean"]
  when:
    - not fstests_initial_baseline|bool

- name: Print uname for each host
  tags: ["oscheck", "fstests", "run_tests"]
  ansible.builtin.debug:
    var: ansible_kernel
- name: Clean up our localhost results/last-run directory
  ansible.builtin.file:
    path: "{{ fstests_results_target }}/"
    state: absent
    mode: "0644"
  delegate_to: localhost
  run_once: true
  tags: ["oscheck", "fstests", "copy_results", "clean_local_results", "do_clean"]

- name: Create empty last-run directory
  ansible.builtin.file:
    path: "{{ fstests_results_target }}/"
    state: directory
    mode: "0755"
  delegate_to: localhost
  run_once: true
  tags: ["oscheck", "fstests", "copy_results", "clean_local_results", "do_deploy"]

- name: Get used target kernel version
  tags: ["oscheck", "fstests", "copy_results", "print_results", "augment_expunge_list"]
  ansible.builtin.command: "uname -r"
  register: fstests_uname_cmd
  changed_when: false

- name: Store last kernel variable
  ansible.builtin.set_fact:
    fstests_last_kernel: "{{ fstests_uname_cmd.stdout_lines | regex_replace('\\]') | regex_replace('\\[') | replace(\"'\", '') }}"
  tags: ["oscheck", "fstests", "copy_results", "print_results", "augment_expunge_list"]
  run_once: true

- name: Document used target kernel version
  ansible.builtin.shell: echo {{ fstests_last_kernel }} > {{ fstests_results_target }}/../last-kernel.txt
  delegate_to: localhost
  tags: ["oscheck", "fstests", "copy_results", "print_results", "augment_expunge_list"]
  run_once: true
  changed_when: false

- name: Ensure systemd-journal-remote.service is running on the server
  tags: ["oscheck", "fstests", "run_tests"]
  delegate_to: localhost
  become_method: ansible.builtin.sudo
  become: true
  become_flags: "su - -c"
  ansible.builtin.systemd_service:
    name: systemd-journal-remote.service
    state: started
  when:
    - devconfig_enable_systemd_journal_remote | default(false) | bool

- name: Ensure systemd-journal-upload.service is running
  tags: ["oscheck", "fstests", "run_tests"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.systemd_service:
    name: systemd-journal-upload.service
    state: started
  when:
    - devconfig_enable_systemd_journal_remote | default(false) | bool
    - "'nfsd' not in group_names"

- name: Hint to watchdog tests are about to kick off
  ansible.builtin.file:
    path: "{{ fstests_workflow_dir }}/.begin"
    state: touch
    mode: "0644"
  delegate_to: localhost
  tags: ["oscheck", "fstests", "run_tests"]
  when:
    - kdevops_run_fstests | default(false) | bool
  run_once: true

- name: Enable low-level messages on console
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.command: sysctl kernel.printk="8 8 1 7"
  changed_when: true
- name: Check to see if dynamic test limit scope was set
  ansible.builtin.set_fact:
    fstests_dynamic_limit_tests: "{{ lookup('ansible.builtin.env', 'LIMIT_TESTS') }}"
  tags: ["oscheck", "fstests", "run_tests", "vars", "limit-tests"]
  when:
    - not run_tests_on_failures | default(false) | bool
    - kdevops_run_fstests | default(false) | bool

- name: Set test limit scope from CONFIG_FSTESTS_RUN_CUSTOM_TESTS, this may be empty
  ansible.builtin.set_fact:
    fstests_all_limit_tests: "{{ limit_tests }}"
  tags: ["oscheck", "fstests", "run_tests", "vars", "limit-tests"]
  when:
    - not run_tests_on_failures | default(false) | bool
    - kdevops_run_fstests | default(false) | bool
    - limit_tests is defined
    - limit_tests != None

- name: Allow dynamic test override
  ansible.builtin.set_fact:
    fstests_all_limit_tests: "{{ fstests_dynamic_limit_tests }}"
  tags: ["oscheck", "fstests", "run_tests", "vars", "limit-tests"]
  when:
    - not run_tests_on_failures | default(false) | bool
    - kdevops_run_fstests | default(false) | bool
    - fstests_dynamic_limit_tests is defined
    - fstests_dynamic_limit_tests != None
    - fstests_dynamic_limit_tests | length > 0

- name: Empty out test limit if None is set
  ansible.builtin.set_fact:
    fstests_all_limit_tests: ""
  tags: ["oscheck", "fstests", "run_tests", "vars", "limit-tests"]
  when:
    - not run_tests_on_failures | default(false) | bool
    - kdevops_run_fstests | default(false) | bool
    - fstests_all_limit_tests is defined
    - fstests_all_limit_tests == None

- name: Run oscheck-get-failures.sh to get list of known failed tests
  vars:
    fstests_section: >-
      {{ (ansible_host | default(inventory_hostname))
         | regex_replace(kdevops_host_prefix + '-')
         | regex_replace('-dev') | regex_replace('-', '_') }}
  tags: ["oscheck", "fstests", "run_tests"]
  ansible.builtin.command: "{{ kdevops_data }}/workflows/fstests/scripts/oscheck-get-failures.sh --test-section {{ fstests_section }}"
  args:
    chdir: "{{ fstests_data_target }}"
  environment:
    FSTYP: "{{ fstests_fstyp }}"
    FSTESTS_LINUX_LOCALVERSION: "{{ target_linux_localversion | default('') }}"
    KEEP_DMESG: "yes"
  register: fstests_failed_tests_limit
  when:
    - run_tests_on_failures | default(false) | bool
    - kdevops_run_fstests | default(false) | bool
  changed_when: true

- name: Inform user no failures are known
  ansible.builtin.fail:
    msg: "No known failures for this run! Go find bugs!"
  tags: ["oscheck", "fstests", "run_tests", "vars", "limit-tests"]
  when:
    - run_tests_on_failures | default(false) | bool
    - kdevops_run_fstests | default(false) | bool
    - fstests_failed_tests_limit.stdout | replace('\n', ' ') | length == 0

- name: Set test limit to known failed tests
  ansible.builtin.set_fact:
    fstests_all_limit_tests: "{{ fstests_failed_tests_limit.stdout | replace('\n', ' ') }}"
  tags: ["oscheck", "fstests", "run_tests", "vars", "limit-tests"]
  when:
    - run_tests_on_failures | default(false) | bool
    - kdevops_run_fstests | default(false) | bool

- name: Print test list limits for this run
  ansible.builtin.debug:
    msg: "fstests_all_limit_tests: {{ fstests_all_limit_tests }}"
  tags: ["oscheck", "fstests", "run_tests", "vars", "limit-tests"]
  when:
    - kdevops_run_fstests | default(false) | bool
    - fstests_all_limit_tests is defined
    - fstests_all_limit_tests != None
    - fstests_all_limit_tests | length > 0

- name: Stop fstests if we're debbugging limit_tests
  ansible.builtin.meta: end_play
  tags: ["oscheck", "fstests", "run_tests", "vars", "limit-tests"]
  when:
    - fstests_skip_run|bool

- name: Start the monitoring services on the test hosts
  ansible.builtin.import_tasks: ../../monitoring/tasks/monitor_run.yml
  when:
    - kdevops_run_fstests | default(false) | bool
    - enable_monitoring|default(false)|bool
  tags: ["oscheck", "fstests", "run_tests", "monitoring", "monitor_run"]

# Recent environments runs are showing that environment variables
# set below are not propagated. So best to stuff what you need
# into the .fstests_setup file which is sourced by root.
- name: Run fstests via oscheck.sh on the current section
  vars:
    fstests_section: >-
      {{ (ansible_host | default(inventory_hostname))
         | regex_replace(kdevops_host_prefix + '-')
         | regex_replace('-dev') | regex_replace('-', '_') }}
    initial_baseline_expunges: "{{ fstests_initial_baseline_args | trim }}"
    skip_tests_args: "{{ fstests_skip_tests_args | trim }}"
  tags: ["oscheck", "fstests", "run_tests"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.command: |
    ./oscheck.sh
      --print-start
      --print-done
      --journal {{ fstests_journal }}
      --test-section
      {{ fstests_section }}
      {{ oscheck_extra_args | default('') }}
      {{ fstests_all_limit_tests }}
      {{ initial_baseline_expunges }}
      {{ skip_tests_args }}
  failed_when: false
  changed_when: true
  no_log: true
  args:
    chdir: "{{ fstests_data_target }}"
  environment:
    FSTYP: "{{ fstests_fstyp }}"
    FSTESTS_SPARSE_FILE_PATH: "{{ fstests_sparsefiles_path }}"
    FSTESTS_SETUP_SYSTEM: "{{ fstests_setup_system }}"
    FSTESTS_LINUX_LOCALVERSION: "{{ target_linux_localversion | default('') }}"
    FSTESTS_TESTDEV_SPARSEFILE_GENERATION: "{{ 'y' if fstests_testdev_sparsefile_generation | default(false) | bool else 'n' }}"
    FSTESTS_SPARSE_FILE_SIZE: "{{ fstests_sparsefiles_size }}"
    FSTESTS_SPARSE_FILENAME_PREFIX: "{{ fstests_sparsefiles_filename_prefix }}"
    FSTESTS_RUN_LARGE_DISK_TESTS: "{{ fstests_run_large_disk_tests | default('') }}"
    FSTESTS_RUN_AUTO_GROUP_TESTS: "{{ fstests_run_auto_group_tests }}"
    FSTESTS_RUN_CUSTOM_GROUP_TESTS: "{{ fstests_run_custom_group_tests }}"
    FSTESTS_EXCLUDE_TEST_GROUPS: "{{ fstests_exclude_test_groups }}"
    LIMIT_TESTS: "{{ fstests_all_limit_tests }}"
    TEST_DEV: "{{ fstests_test_dev }}"
    TEST_DIR: "{{ fstests_test_dir }}"
    KEEP_DMESG: "yes"
  when:
    - kdevops_run_fstests | default(false) | bool

- name: Stop monitoring services and collect data
  ansible.builtin.import_tasks: ../../monitoring/tasks/monitor_collect.yml
  when:
    - kdevops_run_fstests | default(false) | bool
    - enable_monitoring|default(false)|bool
  tags: ["oscheck", "fstests", "run_tests", "monitoring", "monitor_collect"]

- name: Remove watchdog hint that tests have started
  ansible.builtin.file:
    path: "{{ fstests_workflow_dir }}/.begin"
    state: absent
    mode: "0644"
  delegate_to: localhost
  tags: ["oscheck", "fstests", "run_tests", "do_clean"]
  when:
    - kdevops_run_fstests | default(false) | bool
  run_once: true

- name: Look for the check.time files on {{ fstests_data_target }}
  tags: ["oscheck", "fstests", "copy_results", "checktime"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: "{{ fstests_data_target }}"
    recurse: true
    patterns: "check.time"
  register: fstests_checktime_files

- name: Copy the latest run check.time files from {{ fstests_data_target }}
  tags: ["oscheck", "fstests", "copy_results", "checktime"]
  vars:
    item_path_without_host: "{{ item.path | regex_replace('results') | regex_replace(ansible_host) }}"
  ansible.builtin.fetch:
    src: "{{ item.path }}"
    dest: "{{ fstests_results_target }}/{{ item_path_without_host | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
    flat: true
    validate_checksum: false
    mode: "0644"
  with_items: "{{ fstests_checktime_files.files }}"
  loop_control:
    label: "{{ item.path | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
  when:
    - fstests_checktime_files.matched > 0

- name: Look for the check.log files on {{ fstests_data_target }}
  tags: ["oscheck", "fstests", "copy_results", "checklog"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: "{{ fstests_data_target }}"
    recurse: true
    patterns: "check.log"
  register: fstests_checklog_files

- name: Copy the latest run check.log files from {{ fstests_data_target }}
  tags: ["oscheck", "fstests", "copy_results", "checklog"]
  vars:
    item_path_without_host: "{{ item.path | regex_replace('results') | regex_replace(ansible_host) }}"
  ansible.builtin.fetch:
    src: "{{ item.path }}"
    dest: "{{ fstests_results_target }}/{{ item_path_without_host | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
    flat: true
    validate_checksum: false
    mode: "0644"
  with_items: "{{ fstests_checklog_files.files }}"
  loop_control:
    label: "{{ item.path | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
  when:
    - fstests_checklog_files.matched > 0

- name: Look for xunit results files on {{ fstests_data_target }}
  tags: ["oscheck", "fstests", "copy_results", "xunit"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: "{{ fstests_data_target }}"
    recurse: true
    patterns: "*.xml"
  register: fstests_xunit_files

- name: Copy xunit results files from {{ fstests_data_target }}
  tags: ["oscheck", "fstests", "copy_results", "xunit"]
  vars:
    item_path_without_host: "{{ item.path | regex_replace('results') | regex_replace(ansible_host) }}"
  ansible.builtin.fetch:
    src: "{{ item.path }}"
    dest: "{{ fstests_results_target }}/{{ item_path_without_host | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
    flat: true
    validate_checksum: false
    mode: "0644"
  with_items: "{{ fstests_xunit_files.files }}"
  loop_control:
    label: "{{ item.path | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
  when:
    - fstests_xunit_files.matched > 0

- name: Look for tests which failed
  tags: ["oscheck", "fstests", "copy_results", "fstests_failed_tests"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: "{{ fstests_data_target }}"
    recurse: true
    patterns: "*.out.bad"
  register: fstests_failed_tests

- name: Look for TFB results files
  tags: ["copy_results", "fstests_failed_tests", "tfb"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: "{{ fstests_data_target }}/results/"
    recurse: true
    patterns: "*"
    size: "{{ fstests_tfp_copy_size_limit_bytes }}"
    excludes: "*.xml,*.md"
  register: fstests_tfb_reg
  when:
    - fstests_tfp_copy_enable_list|bool or fstests_tfp_copy_enable|bool

- name: Show TFB results files
  tags: ["copy_results", "fstests_failed_tests", "tfb"]
  ansible.builtin.debug:
    msg: "{{ item.path }}"
  with_items: "{{ fstests_tfb_reg.files }}"
  loop_control:
    label: "{{ item.path }} {{ item.size | human_readable(true) }}"
  when:
    - fstests_tfp_copy_enable_list|bool

- name: Show TFB results total files greater than {{ fstests_tfp_copy_size_limit_bytes | human_readable(true) }}
  tags: ["copy_results", "fstests_failed_tests", "tfb"]
  ansible.builtin.debug:
    msg: "Examined {{ fstests_tfb_reg.examined }} files and TFB number of files: {{ fstests_tfb_reg.matched }}"
  when:
    - fstests_tfp_copy_enable_list|bool

- name: Truncate TFB files
  tags: ["copy_results", "fstests_failed_tests", "tfb", "do_deploy"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.copy:
    content: "kdevops TFB trimming enabled, original file size: {{ item.size | human_readable(true) }}"
    dest: "{{ item.path }}"
    mode: "0644"
  with_items: "{{ fstests_tfb_reg.files }}"
  loop_control:
    label: "{{ item.path }} {{ item.size | human_readable(true) }}"
  when:
    - fstests_tfp_copy_enable|bool

# If we want to later add support to copy *all* results file we may
# want to consider removing *.md TFB files. Use 'future' tag for these
# sort of things.
- name: Look for TFB results *.md files
  tags: ["copy_results", "fstests_failed_tests", "tfb", "future"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.find:
    paths: "{{ fstests_data_target }}/results/"
    recurse: true
    patterns: "*.md"
    size: "{{ fstests_tfp_copy_size_limit_bytes }}"
  register: fstests_tfb_reg
  when:
    - false
    - fstests_tfp_copy_enable_list|bool or fstests_tfp_copy_enable|bool

- name: Remove TFB results *.md files
  tags: ["copy_results", "fstests_failed_tests", "tfb", "future"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: "absent"
    mode: "0644"
  with_items: "{{ fstests_tfb_reg.files }}"
  loop_control:
    label: "{{ item.path }} {{ item.size | human_readable(true) }}"
  when:
    - false
    - fstests_tfp_copy_enable_list|bool or fstests_tfp_copy_enable|bool

- name: Copy over all *.bad files which represent failed tests
  tags: ["oscheck", "fstests", "copy_results", "copy_final", "fstests_failed_tests", "tfb"]
  vars:
    item_path_without_host: "{{ item.path | regex_replace('results') | regex_replace(ansible_host) }}"
  ansible.builtin.fetch:
    src: "{{ item.path }}"
    dest: "{{ fstests_results_target }}/{{ item_path_without_host | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
    flat: true
    validate_checksum: false
    mode: "0644"
  with_items: "{{ fstests_failed_tests.files }}"
  loop_control:
    label: "{{ item.path | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
  when:
    - not fstests_tfp_copy_enable_list|bool
    - fstests_failed_tests.matched > 0

- name: Check if respective bad full file exists
  tags: ["oscheck", "fstests", "copy_results", "fstests_failed_tests"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  vars:
    tests_full_group_dir: "{{ item.path | dirname }}"
    tests_group_dir: "{{ tests_full_group_dir | split('/') | last }}"
    tests_full_name: "{{ item.path | basename | split('.') | first }}.full"
    tests_name: "{{ tests_group_dir }}/{{ tests_full_name }}"
  ansible.builtin.stat:
    path: "{{ tests_full_group_dir }}/{{ tests_full_name }}"
  with_items: "{{ fstests_failed_tests.files }}"
  loop_control:
    label: "Looking for {{ tests_full_group_dir }}/{{ tests_full_name }}"
  register: fstests_bad_full_file_stats
  when:
    - fstests_failed_tests.matched > 0

- name: Copy test full bad result file over
  tags: ["oscheck", "fstests", "copy_results", "fstests_failed_tests"]
  vars:
    tests_full_group_dir: "{{ item.item.path | dirname }}"
    tests_group_dir: "{{ tests_full_group_dir | split('/') | last }}"
    tests_full_name: "{{ item.item.path | basename | split('.') | first }}.full"
    tests_name: "{{ tests_group_dir }}/{{ tests_full_name }}"
    item_path_without_host: "{{ item.stat.path | regex_replace('results') | regex_replace(ansible_host) }}"
  ansible.builtin.fetch:
    src: "{{ item.stat.path }}"
    dest: "{{ fstests_results_target }}/{{ item_path_without_host | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
    flat: true
    validate_checksum: false
    mode: "0644"
  with_items: "{{ fstests_bad_full_file_stats.results }}"
  loop_control:
    label: "Copying over {{ tests_name }}"
  when:
    - fstests_failed_tests.matched > 0
    - item.stat.exists

- name: Check if respective dmesg file exists for failed tests
  tags: ["oscheck", "fstests", "copy_results", "fstests_failed_tests"]
  become: true
  become_flags: "su - -c"
  become_method: ansible.builtin.sudo
  vars:
    tests_full_group_dir: "{{ item.path | dirname }}"
    tests_group_dir: "{{ tests_full_group_dir | split('/') | last }}"
    tests_dmesg_name: "{{ item.path | basename | split('.') | first }}.dmesg"
    tests_name: "{{ tests_group_dir }}/{{ tests_dmesg_name }}"
  ansible.builtin.stat:
    path: "{{ tests_full_group_dir }}/{{ tests_dmesg_name }}"
  with_items: "{{ fstests_failed_tests.files }}"
  loop_control:
    label: "Looking for {{ tests_full_group_dir }}/{{ tests_dmesg_name }}"
  register: fstests_bad_dmesg_file_stats
  when:
    - fstests_failed_tests.matched > 0

- name: Copy test dmesg results file over
  tags: ["oscheck", "fstests", "copy_results", "fstests_failed_tests"]
  vars:
    tests_full_group_dir: "{{ item.item.path | dirname }}"
    tests_group_dir: "{{ tests_full_group_dir | split('/') | last }}"
    tests_dmesg_name: "{{ item.item.path | basename | split('.') | first }}.dmesg"
    tests_name: "{{ tests_group_dir }}/{{ tests_dmesg_name }}"
    item_path_without_host: "{{ item.stat.path | regex_replace('results') | regex_replace(ansible_host) }}"
  ansible.builtin.fetch:
    src: "{{ item.stat.path }}"
    dest: "{{ fstests_results_target }}/{{ item_path_without_host | regex_replace(fstests_data_target | regex_escape()) | regex_replace('^/', '') }}"
    flat: true
    validate_checksum: false
    mode: "0644"
  with_items: "{{ fstests_bad_dmesg_file_stats.results }}"
  loop_control:
    label: "Copying over dmesg {{ tests_name }}"
  when:
    - fstests_failed_tests.matched > 0
    - item.stat.exists

- name: Print fstests results to xunit_results.txt on localhost if xunit xml file was found
  ansible.builtin.command: >-
    ./python/workflows/fstests/gen_results_summary
    --results_file result.xml --print_section
    --output_file {{ fstests_results_target }}/{{ fstests_last_kernel }}/xunit_results.txt
    {{ fstests_results_target }}/
  delegate_to: localhost
  tags: ["oscheck", "fstests", "copy_results", "print_results", "augment_expunge_list"]
  when:
    - fstests_xunit_files.matched > 0
  run_once: true
  changed_when: false

- name: Print fstests bad results to bad_results.txt on localhost
  ansible.builtin.shell: >-
    ./python/workflows/fstests/bad_files_summary.py {{ fstests_fstyp }} {{ fstests_results_target }}/
    > {{ fstests_results_target }}/bad_results.txt
  delegate_to: localhost
  tags: ["oscheck", "fstests", "copy_results", "print_results", "augment_expunge_list"]
  when:
    - fstests_failed_tests.matched > 0
  run_once: true
  changed_when: false

- name: Create check.time.distribution files for the local fstests results
  ansible.builtin.command: ./python/workflows/fstests/fstests-checktime-distribution.py {{ fstests_results_target }}
  delegate_to: localhost
  tags: ["oscheck", "fstests", "copy_results", "augment_expunge_list"]
  run_once: true
  when:
    - fstests_checktime_files.matched > 0
  changed_when: true

- name: Augment expunge list; run git diff after this to see new failures
  ansible.builtin.command: >-
    ./python/workflows/fstests/augment_expunge_list.py
    {{ fstests_fstyp }} {{ fstests_results_target }}/
    {{ fstests_workflow_dir }}/expunges/
  delegate_to: localhost
  tags: ["oscheck", "fstests", "copy_results", "augment_expunge_list"]
  run_once: true
  changed_when: true

- name: Print fstests new expunge files not yet committed
  ansible.builtin.shell: >-
    ./python/workflows/fstests/get_new_expunge_files.py
    {{ fstests_fstyp }} {{ fstests_workflow_dir }}/expunges/
    > {{ fstests_results_target }}/new_expunge_files.txt
  delegate_to: localhost
  tags: ["oscheck", "fstests", "copy_results", "print_results", "augment_expunge_list"]
  run_once: true
  changed_when: false

- name: Get stat of new files not yet committed, ie, new expunge files
  ansible.builtin.stat:
    path: "{{ fstests_results_target }}/new_expunge_files.txt"
  delegate_to: localhost
  register: fstests_new_expunge_files_stat
  run_once: true
  tags: ["oscheck", "fstests", "copy_results", "print_results", "augment_expunge_list"]

- name: Inform user if new expunge files have been found
  run_once: true
  tags: ["oscheck", "fstests", "copy_results", "augment_expunge_list"]
  ansible.builtin.debug:
    msg: "New expunge files found in: {{ fstests_results_target | regex_replace('^../', '') }}new_expunge_files.txt"
  when:
    - fstests_new_expunge_files_stat.stat.size != 0

- name: Inform user if no failures have been found
  tags: ["oscheck", "fstests", "copy_results", "augment_expunge_list"]
  ansible.builtin.debug:
    msg: No *.bad or xunit xml files were found on the target host
  when:
    - fstests_failed_tests.matched == 0
    - fstests_xunit_files.matched == 0
    - fstests_new_expunge_files_stat.stat.size != 0

- name: Verify last-run kernel directory exists
  tags: ["oscheck", "fstests", "copy_results", "augment_expunge_list"]
  delegate_to: localhost
  ansible.builtin.stat:
    path: "{{ fstests_results_full_path }}/last-run/{{ fstests_last_kernel }}"
  register: fstests_last_run_kernel_dir

- name: Copy over the last-run into the results directory for archiving into kdevops
  ansible.builtin.copy:
    src: "{{ fstests_results_full_path }}/last-run/{{ fstests_last_kernel }}"
    dest: "{{ fstests_results_full_path }}/"
    mode: "0644"
  run_once: true
  tags: ["oscheck", "fstests", "copy_results", "augment_expunge_list", "do_deploy"]
  delegate_to: localhost
  when:
    - fstests_last_run_kernel_dir.stat.exists

# You are encouraged to commit the compressed results of failed tests
# so we keep an archive
- name: Compress the per-kernel results archive
  community.general.archive:
    path: "{{ fstests_results_full_path }}/{{ fstests_last_kernel }}"
    dest: "{{ fstests_results_full_path }}/{{ fstests_last_kernel }}.xz"
    format: xz
    mode: "0644"
  run_once: true
  tags: ["oscheck", "fstests", "copy_results", "augment_expunge_list"]
  delegate_to: localhost
