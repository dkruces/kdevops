# SPDX-License-Identifier GPL-2.0+
---
# Role-private defaults only. Tree-wide vars (distro_debian_based,
# libvirt_uri_system, libvirt_enable_largeio, bootlinux_9p) come
# from extra_vars.yaml via Kconfig output yaml. Consumers
# reference them with `| default(false)` where the gap case
# applies. See docs/module-spec.md "Configuration flow".
