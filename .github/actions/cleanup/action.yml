# SPDX-License-Identifier: GPL-2.0
---
name: Cleanup kdevops VMs
description: Destroy VMs and cleanup workspace

inputs: {}

runs:
  using: "composite"
  steps:
    - name: Run kdevops make destroy
      shell: bash
      run: |
        set -euxo pipefail
        # make destroy
        echo "destroy"

    - name: Remove working-directory
      shell: bash
      run: |
        set -euxo pipefail
        # No cleanup needed - working in current directory with direct checkout
        echo "Cleanup completed - current directory will be handled by runner"
