# SPDX-License-Identifier: copyleft-next-0.3.1
---
# Role-private defaults only. The tree-wide gates (enable_monitoring,
# monitor_developmental_stats, monitor_folio_migration,
# monitor_folio_migration_interval) come from extra_vars.yaml via
# Make's --extra-vars; consumers reference them with `| default(...)`.
monitoring_results_base_path: "{{ topdir_path }}/workflows/fstests/results/monitoring"
