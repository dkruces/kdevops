{% from "gen_nodes_list.j2" import gen_nodes_list -%}
{{ gen_nodes_list( nodes, kdevops_baseline_and_dev, 100,
		   pcie_passthrough_enable | default(false),
		   pcie_passthrough_target_type | default(''),
		   pcie_passthrough_target | default(''),
		   pcie_passthrough_devices | default([]) ) | replace("None", "") }}
